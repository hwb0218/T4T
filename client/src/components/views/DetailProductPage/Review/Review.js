import React from "react";
import styled from "styled-components";

const ReviewWrapper = styled.div`
  margin-top: 1rem;
`;

const Review = () => {
  return <ReviewWrapper>여기에다 리뷰 작업</ReviewWrapper>;
};

export default Review;
